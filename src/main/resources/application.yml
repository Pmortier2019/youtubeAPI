server:
  port: ${PORT:8080}

logging:
  level:
    root: WARN
    org.springframework.security: WARN
    org.springframework.web: WARN
    org.springframework.mail: DEBUG
    com.example.soundtracker: DEBUG

spring:
  datasource:
    url: jdbc:postgresql://${PGHOST:localhost}:${PGPORT:5432}/${PGDATABASE:soundtracker}
    username: ${PGUSER:soundtracker}
    password: ${PGPASSWORD:soundtracker}
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  mail:
    from: ${MAIL_FROM:onboarding@resend.dev}

app:
  base-url: ${APP_BASE_URL:http://localhost:8080}

resend:
  api-key: ${RESEND_API_KEY:}

encryption:
  key: ${ENCRYPTION_KEY:}

youtube:
  apiKey: ${YOUTUBE_API_KEY:changeme}
  innertubeKey: ${INNERTUBE_SCRAPE_KEY:}
  baseUrl: https://www.googleapis.com/youtube/v3
  batchSize: 50

scheduler:
  enabled: true
  cron: "0 15 2 * * *"

jwt:
  secret: ${JWT_SECRET:changeme-set-a-strong-secret-in-production-min-32-chars}
  expirationMs: 86400000